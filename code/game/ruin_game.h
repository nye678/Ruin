#ifndef __RUIN_GAME_
#define __RUIN_GAME_

namespace Ruin
{    
    enum reGameState
    {

    };

    struct rsGameData
    {
        //CurrentLevel
        //NextLevel
        //Player
    };

    void GameMain()
    {
        // Load the initial level and set to the current level.
        // Start loading next level in background.
        // Begin executing current level.
        // while game running
            // update level
            // if level complete
                // goto next level.
    }
}

#endif